{
  "name": "drill-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "blueprint build --all",
    "test": "blueprint test",
    "deploy:jetton": "blueprint run deployDrillJetton --testnet --tonconnect",
    "deploy:sbt": "blueprint run deployDrillPass --testnet --tonconnect",
    "mint": "blueprint run mintDrill --testnet --tonconnect"
  },
  "devDependencies": {
    "@ton/blueprint": "^0.40.0",
    "@ton/core": "^0.63.0",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.37.0",
    "@ton/test-utils": "^0.11.0",
    "@ton/ton": "^16.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
