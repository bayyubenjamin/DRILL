{
  "name": "drill-contracts",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "blueprint build --all",
    "deploy:jetton": "blueprint run deployDrillJetton --testnet --mnemonic",
    "deploy:sbt": "blueprint run deployDrillPass --testnet --mnemonic",
    "mint": "blueprint run mintDrill --testnet --mnemonic"
  },
  "devDependencies": {
    "@ton/blueprint": "^0.40.0",
    "@ton/core": "^0.63.0",
    "@ton/crypto": "^3.3.0",
    "@ton/sandbox": "^0.37.0",
    "@ton/ton": "^16.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
